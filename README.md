📦 1. Установка Ollama

Установите Ollama согласно официальной документации:

Linux / macOS
```shell
curl -fsSL https://ollama.com/install.sh | sh
```
Windows

Скачать установщик можно на сайте Ollama.

Проверьте, что всё работает:
```shell
ollama --version
```

🤖 2. Загрузка модели и простой локальный запрос

Сначала скачайте модель:
```shell
ollama pull qwen2:0.5b
```

Простой запрос в модель:
```shell
ollama run qwen2:0.5b
```


🐳 3. Запуск API в Docker-контейнере

В проекте есть Dockerfile, который поднимает API бекенда, который делает запросы к модели в Ollama.

Сборка и запуск:
```shell 
sudo docker compose up --build -d api
```

Swagger API будет доступен по адресу:

http://localhost:8000/docs

📘 4. Тестирование API через Swagger

После запуска контейнера откройте в браузере и отправьте тестовый запрос:

http://localhost:8000/docs


🤖 5. Запуск Telegram-бота

В проекте есть Dockerfile, который поднимает tg бот, который делает запросы к API.

Настройка

Получите токен в @BotFather.

В .env создайте:

BOT_TOKEN=ваш_токен

Сборка и запуск:
```shell 
sudo docker compose up --build -d tg_bot
```

🔗 6. Как всё работает вместе

Ollama локально запускает LLM-модель.

Docker-контейнер поднимает API, который делает запросы к Ollama.

Через Swagger вы тестируете работу API.

Telegram-бот получает сообщения пользователя и отправляет их в API.

Ответ модели возвращается пользователю в Telegram.

📂 Структура проекта
```
.
├── README.md
├── .env.template
├── backend
│   ├── Dockerfile
│   ├── main.py
│   ├── pyproject.toml
│   └── uv.lock
├── docker-compose.yaml
└── tg_bot
│    ├── Dockerfile
│    ├── main.py
│    ├── pyproject.toml
│    └── uv.lock
```